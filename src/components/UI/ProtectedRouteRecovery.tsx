import { ReactNode } from "react";
interface ProtectedRouteRecoveryProps {
    children: ReactNode;
  }

export default function ProtectedRouteRecovery({children}:ProtectedRouteRecoveryProps) {
  return children
}
